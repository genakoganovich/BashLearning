{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bashdb",
            "request": "launch",
            "name": "Debug Current File",
            "program": "${file}",
            "args": [],
            "env": {},
            "cwd": "${workspaceFolder}"
        }
    ]
}
